Design Changes

Use inter font for the premium Apple-look.

Color Palette

Component,Color Hex,Description
Primary Background,#0B0B0B to #000000,Pure and near-pure black used for the app cards and settings screens to provide depth.
Accent Glow (Orange-Red),#FF451D to #E63917,"The vibrant, glowing orange-red used for the timer progress ring, the word ""Timer"", and the focus indicators."
Secondary Background,#202020,"A lighter dark-gray used for interactive elements like the START, PAUSE, and DURATION buttons."
Primary Text,#FFFFFF,"Crisp, solid white used for high-importance readouts like the timer digits and screen titles."
Muted Text / Icons,#8E8E93,"A neutral mid-gray used for secondary labels, subtle borders, and inactive icons

Layout and logic changes

User should be able to set the timer manually. Default is 10 minutes. An alarm or a song should play when its done. 

Make the default window size smaller to minimize distraction

Remove the top navbar , remove the sessions and remove the work, short and long tabs. The tabs should be replaced with work and break. there should only be pause and restart buttons. 

Add little numbered icons on the bottom right of the video player to choose between 3 available local videos in the assets/videos folder. The app should randomly play any one of these three videos as default when the app opens.

Replace the done and streak with a work done and breaks taken counter. work count should increase when timer finishes, break count should increase when timer starts . This data should persist throughout multiple sessions.

let me know of additional lofic features that can be implemented or logic flaws within these changes. list both down
 